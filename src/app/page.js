/**
 * Função que representa a página inicial da aplicação.
 * 
 * @returns {JSX.Element} O elemento JSX que representa a página inicial.
 */
function Home() {
  // Retorna o conteúdo da página inicial.
  return (
    <>
      {/* Título da página */}
      <div>Página</div>
    </>
  );
}

// Exporta a função Home como o componente padrão.
export default Home;